apiVersion: v1
kind: Service

{{- $svc := index .Values "user-service" -}}

metadata:
  name: user-service
  namespace: {{.Values.namespace}}
  labels:
    app: user-service

spec:
  selector:
    app: user-service

  ports:
    - port: {{ $svc.service.port }}

      targetPort: {{ $svc.service.port }}

  type: ClusterIP


  